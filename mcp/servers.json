{
  "_comment": "mcp/servers.json — SOURCE. Edit 2 of 2. Canonical MCP server list, human-edited. tools/sync.ps1 derives .vscode/mcp.json, .mcp.json, .codex/config.toml and .gemini/settings.json from this file — never hand-edit those. Ships near-empty: add your team's real servers below. The entry below is a documented example, not a live server; remove or replace it.",
  "servers": {}
}
